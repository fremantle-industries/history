defmodule <%= module_prefix %>.Repo.Migrations.CreateHistoryOpenInterests do
  use Ecto.Migration

  def change do
    create table(:open_interests<%= if not is_nil(db_prefix), do: ", prefix: \"#{db_prefix}\"" %>) do
      add :venue, :string, null: false
      add :symbol, :string, null: false
      add :value, :decimal, null: false

      timestamps()
    end

    create unique_index(:open_interests, [:venue, :symbol])
  end
end
